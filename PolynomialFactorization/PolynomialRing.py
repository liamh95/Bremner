class PolynomialRing:

	def __init__(self, ring_p, indeterminate_symbol):
		self.ring = p
		self.indeterminate_symbol = indeterminate_symbol
		#TODO
		# arbitrary rings
		# check if p is prime
		# multivariate

	def gen(self):
		return Polynomial(self, [0,1])	